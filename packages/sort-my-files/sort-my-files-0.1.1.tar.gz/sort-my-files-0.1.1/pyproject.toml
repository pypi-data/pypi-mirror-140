[tool.poetry]
name = "sort-my-files"
version = "0.1.1"
description = ""
authors = ["Daniel Evans <me@daredoes.work>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
typer = "^0.4.0"

[tool.poetry.dev-dependencies]
pytest = "^5.2"
black = "^22.1.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
sort-my-files = "sort_my_files.main:app"