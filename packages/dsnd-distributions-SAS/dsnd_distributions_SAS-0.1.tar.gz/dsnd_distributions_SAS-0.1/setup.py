from setuptools import setup

setup(name='dsnd_distributions_SAS',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['dsnd_distributions_SAS'],
      author = 'Samara Sadeek',
      zip_safe=False)
