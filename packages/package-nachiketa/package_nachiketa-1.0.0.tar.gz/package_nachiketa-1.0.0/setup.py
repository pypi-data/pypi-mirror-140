from setuptools import setup

setup(
    name="package_nachiketa",
    version="1.0.0",
    description="Code for fibonacci number",
    long_description="It will return n-th fibonacci number",
    author="Nachiketa",
    packages=["package_nachiketa"],
    install_requires=[],
)
